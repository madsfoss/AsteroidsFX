**Build the project from the root folder with the following Maven command:** <br>
```mvn clean install```

**Run the project from root folder with following Maven command:** <br>
``` mvn exec:exec ```

**Run following command to execute the project:** <br>
```java --module-path mods-mvn --class-path "libs/*" --module=Core/dk.sdu.cbse.App```